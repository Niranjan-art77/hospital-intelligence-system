services:
  - type: web
    name: hospital-backend
    runtime: docker
    plan: free
    region: ohio
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: "5000"
      - key: PYTHON_VERSION
        value: "3.11.0"
